#ifndef DEFINES_H
#define DEFINES_H

// Enum of possible errors
enum eError {
    E_SUCCES, E_ALLOCATION_ERROR, E_INVALID_ARGS, E_INVALID_TYPE
};

// Enum of types
enum eType {
    MIN, T_DOUBLE, T_CHAR, T_INT, T_VOID_PTR,  ALL
};

#endif
